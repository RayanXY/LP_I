#include <iostream>
#include <iterator>

int * unique(int *first, int *last){
	
	int *newSize; 
	for(/* Empty */; i != last; /* Empty */){
		if(){

		}
	}
	return newSize;

}

int main(){

	int *i;
	int A[] = {1, 2, 1, 2, 3, 3, 1, 2, 4, 5, 3, 4, 5};

	auto last = unique(std::begin(A), std::end(A));

	for(i = std::begin(A); i != last; i++){
		std::cout << "|" << *i;
	}
	std::cout << "|\n";

}